## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new release.

The package provides tools for feature selection in support vector machine models
using the Non-dominated Sorting Genetic Algorithm II (NSGA-II).

In this version, we have:
* clarified the Description field by explaining the acronym NSGA-II;
* revised the Description text to improve clarity and avoid ambiguous wording;
* updated the package documentation and metadata;
* removed the default output path from `save_results()` to avoid writing to
the user's working directory by default;
* added short examples.

## Notes

There are no known issues.
